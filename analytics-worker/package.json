{
  "name": "bunker-seo-analytics",
  "version": "0.6.0",
  "private": true,
  "type": "module",
  "description": "Phase 6 Search Console analytics worker: read-only sync, labels and opportunity detection.",
  "scripts": {
    "start": "node worker.js",
    "test": "node --test \"test/*.test.js\""
  },
  "dependencies": {
    "pocketbase": "^0.28.1"
  },
  "engines": {
    "node": ">=20"
  }
}
