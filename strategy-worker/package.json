{
  "name": "bunker-seo-strategy-worker",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "description": "Budgeted Phase 3 SEO strategy worker with optional OpenAI discovery.",
  "scripts": {
    "start": "node worker.js",
    "test": "node --test \"test/*.test.js\""
  },
  "dependencies": {
    "pocketbase": "^0.28.1"
  },
  "engines": {
    "node": ">=20"
  }
}
